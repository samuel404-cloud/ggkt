1.关于导入project：
        找到pom.xml所在目录导入即可；
        如果其中的module都失效了，可以点右边栏Maven，有个“+”号，
      依次点开每个module的pom.xml即可识别。

2.module导入也可以参考在File-Project Structure-Module-"+"号-import module
from external model选择maven。如果是web，则Facets-“+”号-Web

3.
